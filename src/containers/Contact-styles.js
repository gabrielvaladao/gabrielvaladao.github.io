import { css } from 'styled-components';

const ContactInfoStyles = css`
  text-align: center;
`;

const AddressStyles = css`
  display: inline;
  font-style: inherit;
`;

export { ContactInfoStyles, AddressStyles };
